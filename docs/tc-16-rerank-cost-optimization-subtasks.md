# TC-16 子任务清单：Rerank 成本优化（窗口化 + 融合打分）

更新时间：2026-04-08

## 1. 方案目标

- 降低 rerank 链路时延与调用成本（重点关注 P95）。
- 保持相关性不回退（Recall@K / nDCG@K）。
- 与已上线分页能力兼容，避免跨页排序抖动。

## 2. 子任务拆分（按执行顺序）

| 序号 | 子任务 | 优先级 | 状态 | 主要产出 | 依赖 | 预估 |
|---|---|---|---|---|---|---|
| ST-01 | 明确优化基线与目标 | P0 | 待执行 | 固化 baseline（P95、Recall@K、nDCG@K、rerank 请求文档数）与目标阈值 | 无 | 0.5 天 |
| ST-02 | 新增 rerank 窗口化配置 | P0 | 待执行 | `window-enabled/window-size/window-factor/window-min/window-max` 配置项与默认值 | ST-01 | 0.5 天 |
| ST-03 | 实现窗口化 rerank 逻辑 | P0 | 待执行 | 仅对窗口候选执行 rerank，非窗口候选保留原检索分 | ST-02 | 0.5-1 天 |
| ST-04 | 实现融合打分策略 | P0 | 待执行 | `finalScore = alpha * retrieval + beta * rerank`（可配置）替代硬覆盖 | ST-03 | 0.5-1 天 |
| ST-05 | 稳定排序与分页兼容校验 | P0 | 待执行 | 同分 tie-break（如 `id ASC`）与分页一致性验证策略 | ST-04 | 0.5 天 |
| ST-06 | 监控与观测补齐 | P1 | 待执行 | 增加 rerank 窗口命中率、平均窗口大小、降级次数、耗时指标 | ST-03~05 | 0.5 天 |
| ST-07 | 测试与回归 | P0 | 待执行 | 单测覆盖窗口/融合/降级/边界；核心回归通过 | ST-03~06 | 0.5-1 天 |
| ST-08 | 灰度发布与参数调优 | P0 | 待执行 | 小流量灰度 + 参数对比报告（延迟/质量双指标） | ST-07 | 0.5 天 |

## 3. 推荐默认参数（V1）

- `window-enabled: true`
- `window-factor: 3`
- `window-min: 20`
- `window-max: 80`
- `window-size: 40`（当未显式传入时作为兜底）
- `fusion-alpha: 0.6`
- `fusion-beta: 0.4`

说明：以上参数是冷启动默认值，最终以 `TC-13` 离线评测结果与线上灰度数据为准。

## 4. 影响面分析

- 主要影响：`HybridRetrievalStrategy` 的 rerank 逻辑和排序输出。
- 配置影响：`application.yaml` 增加窗口与融合参数。
- 观测影响：新增指标与日志字段，支持灰度对比。
- 兼容性：不改变外部 API 契约；分页接口可复用首查排序结果。

## 5. 边界说明

- 边界内：窗口化 rerank、融合打分、参数配置化、监控与回归、灰度调优。
- 边界外：更换 rerank 模型、重构检索召回链路、全自动 MLOps 平台化。

## 6. DoD（验收标准）

1. 与 baseline 对比，P95 下降（目标 20%+）。
2. Recall@K / nDCG@K 不明显回退（阈值在 ST-01 固化）。
3. 分页连续翻页顺序稳定，无重复无漏项。
4. rerank 异常时可自动降级并有可观测日志/指标。
5. 配置可动态调整，测试与回归通过。
